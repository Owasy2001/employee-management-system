@page
@using SAASShopDomain;
@using SAASShop.Pages.Shared
@model SAASShop.Pages.Management.AddLeaveBalanceModel
@{
    ViewData["Title"] = "Leave Balence";
}
@section SideMenu {
    @{
        await Html.RenderPartialAsync("_DashboardMenuPartial", new MenuModel { Identifier = "leavemanage" });
    }
}

@* @{
    var userName = User.Claims.FirstOrDefault(x => x.Type == System.Security.Claims.ClaimTypes.GivenName).Value;
}

<partial name="_DataFormTitlePartial" model='new string($"Add Leave Application for {userName}")' /> *@
<div class="card dataForm">
    <form method="Post">
        <partial name="_LeaveBalancePartial" model="Model.leaveBalance" />

        <div class="rowborder-top p-2">
            <div class="col-md-12 text-right">
                <hr />
                <button type="submit" class="btn btn-blueTwo ms-2 float-end"> Save </button>
                <a href="@Navigator.AddLeaveBalance" class="btn btn-sliverOne float-end">Cancel</a>
            </div>
        </div>
    </form>
</div>